# The Vite dev server proxies this same-origin path to RIFT_CONTROL_API.
VITE_RIFT_CONTROLLER_URL=/api/rift

# Preview values are clearly labeled and only fill future controller surfaces.
VITE_RIFT_PREVIEW_DATA=true
